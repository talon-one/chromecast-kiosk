[
    {
        "Name": "Chromecast",            // Exact name of the Chromecast device (use this or the `Host' setting, `Host' will be prefered if specified)
        "URL": "https://example.com",    // URL to cast
        "Timeout": 60000,                // Re(Cast) & Reconnect every 60 seconds (removing results in default value (60000))
        "Reload": 1800000,               // Reload the website every 30 minutes (remove or set to 0 to disable)
    },
    {
        "Host": "192.168.101.251",       // Host or IP address of the chromecast (use this or the `Name' setting, `Host' will be prefered if specified
        "URL": "https://example.com",    // URL to cast
        "Timeout": 60000,                // Re(Cast) & Reconnect every 60 seconds (removing results in default value (60000))
        "Reload": 1800000,               // Reload the website every 30 minutes (remove or set to 0 to disable)
    },
]